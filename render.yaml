services:
  - type: web
    name: time-api
    env: docker
    rootDir: .
    dockerfilePath: Dockerfile
    dockerContext: .
    autoDeploy: true
    plan: free
    healthCheckPath: /health
    envVars:
      - key: TIMEZONE
        value: America/Argentina/Buenos_Aires
